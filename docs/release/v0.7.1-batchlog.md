# v0.7.1 Batchlog

Date: 2026-05-20

## Batch Scope

- Destination audit across OpenClaw, Hermes/Nimbus, Codex, Claude Code/Desktop, MCP clients, Cursor/Windsurf, manual cloud imports, and Memory Passport.
- MCP drift fix for package-version reporting, tool annotations, stricter source/status schemas, and structured-output regression coverage.
- Release preparation for GitHub release and npm package version `0.7.1`.

## Decisions

- Keep npm for v0.7.1 because the repo already uses `package-lock.json`, npm scripts, npm dry-run packaging, and npm publish flow.
- Do not introduce pnpm in this release; evaluate it separately if the repo becomes a workspace or monorepo.
- Treat Hermes native memory as a hot cache and Gigabrain as the long-term control plane.
- Treat closed cloud products as explicit manual import/export only.

## Validation Targets

- `npm outdated`: only optional `openclaw` peer latest remained outside installed dependencies.
- `npm test`: passed full suite.
- `npm audit --audit-level=high`: passed with 0 vulnerabilities.
- `npm pack --dry-run`: passed and included destination audit, batchlog, and v0.7.1 release notes.
- `npm publish --dry-run`: passed without package autocorrect warnings after bin path normalization.
- `npm run test:release-live`: passed Codex CLI and OpenClaw packaged install smokes.
- `hermes mcp test gigabrain`: passed with 12 tools discovered.
- Hermes CLI recall smoke: returned the imported Nimbus Telegram memory.
- `gigabrainctl sync-hosts status`: showed Hermes and OpenClaw synced read-only.

## Known Release Blockers

- `npm whoami` returns `E401 Unauthorized`; real npm publish requires `npm login --auth-type=web`.
- PR #63 could not be merged by CLI because branch policy blocks direct merges and repo auto-merge is disabled. The v0.7.1 branch is stacked on the green PR #63 head so the final PR contains the bridge contract plus this release audit.
- PR #64 passed CI and CodeQL but also cannot be merged by CLI: branch policy blocks direct merges and repository auto-merge is disabled. Tagging `v0.7.1` and creating the GitHub release should wait until an authorized maintainer merges the PR.

## Release Notes

- GitHub release target: `v0.7.1`
- npm target: `@legendaryvibecoder/gigabrain@0.7.1`
- npm publish is gated on successful `npm login --auth-type=web`.
