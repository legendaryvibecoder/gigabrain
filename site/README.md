# Gigabrain Launch Site

Static marketing site for the Memory Passport pivot.

- `index.html` is standalone and can be served by any static host.
- `assets/passport-report-demo.png` is generated from the local demo Passport HTML during verification.

